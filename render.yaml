services:
  - type: web
    name: yalla-foot
    env: node
    nodeVersion: 18.x
    buildCommand: |
      npm ci
      npm run build
    startCommand: npm run serve
    envVars:
      - key: NODE_ENV
        value: production
